/*  (if you want the user to only enter valid words)
function isWordValid(word){
    return dictionary.includes(word);
}

if (key === 'Enter') 
{
    if(state.currentCol === 5) {
        const word = getCurrentWord();
        if (isWordValid(word)) {
            revealWord(word);
            state.currentRow++;
            state.currentCol = 0;
        } 
        else{
            alert('Not a valid word.');
        }
    }
}
*/


